## SyncWire EMQX topic ACL (Phase 0 placeholder)
##
## Phase 0: allow-all. Phase 2 replaces this file with:
##   - per-device publish rights to syncwire/relay/${clientid}
##   - per-device subscribe rights to syncwire/inbox/${clientid}
##   - read-only subscribe to syncwire/status/+
##   - a privileged bridge@system account that can publish to any inbox
##
## Mounted at /opt/emqx/etc/acl.conf and referenced by emqx.conf.

%% This file is intentionally permissive in v0.
%% See plan §5 for the production ACL design.
{allow, all, all, ["#"]}.
